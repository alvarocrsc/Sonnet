<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <!-- Delete Button -->
    <ImageView
        android:id="@+id/delete_button"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:src="@drawable/ic_delete"
        android:clickable="true"
        android:focusable="true"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        app:tint="#FFFFFF"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/file_name"
        app:layout_constraintBottom_toBottomOf="@id/stream_count" />

    <!-- File Name -->
    <TextView
        android:id="@+id/file_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:fontFamily="@font/geist_medium"
        android:text="Streaming_History_Audio_2023-2024_6.json"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:letterSpacing="-0.025"
        android:ellipsize="end"
        android:maxLines="1"
        tools:ignore="HardcodedText"
        app:layout_constraintStart_toEndOf="@id/delete_button"
        app:layout_constraintEnd_toStartOf="@id/status_container"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Import Date -->
    <TextView
        android:id="@+id/import_date"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/geist_light"
        android:text="Imported on 1/23/24 at 17:46"
        android:textColor="#A5A5A5"
        android:textSize="11sp"
        android:letterSpacing="-0.03"
        android:layout_marginTop="-5dp"
        tools:ignore="HardcodedText"
        app:layout_constraintStart_toStartOf="@id/file_name"
        app:layout_constraintEnd_toEndOf="@id/file_name"
        app:layout_constraintTop_toBottomOf="@id/file_name" />

    <!-- Stream Count -->
    <TextView
        android:id="@+id/stream_count"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:fontFamily="@font/geist_light"
        android:text="8,664 verified streams"
        android:textColor="#A5A5A5"
        android:textSize="11sp"
        android:letterSpacing="-0.03"
        android:layout_marginTop="-5dp"
        tools:ignore="HardcodedText"
        app:layout_constraintStart_toStartOf="@id/file_name"
        app:layout_constraintEnd_toEndOf="@id/file_name"
        app:layout_constraintTop_toBottomOf="@id/import_date" />

    <!-- Status Container (Progress or Icon) -->
    <FrameLayout
        android:id="@+id/status_container"
        android:layout_width="21dp"
        android:layout_height="21dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/file_name"
        app:layout_constraintBottom_toBottomOf="@id/stream_count">

        <!-- Progress Indicator (shown during import) -->
        <ProgressBar
            android:id="@+id/progress_indicator"
            android:layout_width="21dp"
            android:layout_height="21dp"
            android:visibility="gone"
            android:indeterminateTint="#FFFFFF" />

        <!-- Status Icon (shown when complete/error) -->
        <ImageView
            android:id="@+id/status_icon"
            android:layout_width="21dp"
            android:layout_height="21dp"
            android:visibility="gone"
            android:src="@drawable/ic_success" />
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
